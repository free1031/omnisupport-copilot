select
    ticket_id,
    tenant_id,
    org_id,
    org_name,
    status,
    priority,
    category,
    product_line,
    product_version,
    assignee_id,
    sla_tier,
    sla_due_at,
    created_at,
    created_date,
    updated_at,
    resolved_at,
    first_comment_at,
    first_response_minutes,
    handle_time_minutes,
    backlog_age_days,
    is_open,
    is_resolved,
    is_escalated,
    is_p1,
    sla_breached,
    is_first_resolution_proxy,
    pii_level,
    pii_redacted,
    data_release_id,
    ingest_batch_id,
    schema_version
from {{ ref('int_support_cases') }}
